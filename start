#!/bin/bash
set -e

trap "killall mongod" EXIT

cd $(dirname $0)

(mongod --dbpath data/mongodb/db) &

(redis-server /usr/local/etc/redis.conf) &

DEBUG=true npm run postinstall

eval $(cat .env)

(. ./prestart.sh && NODE_ENV=development foreman start -f Procfile.dev)
