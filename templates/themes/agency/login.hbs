<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
	<meta name="HandheldFriendly" content="true">
	<meta name="description" content="">
	<meta name="author" content="">
	<link rel="shortcut icon" href="{{ resourceUrl 'favicon.ico' }}"/>
	<title>{{ theme.browser.title }}</title>
	<link href='//fonts.googleapis.com/css?family=Montserrat:400,700|Open+Sans:400,300,700' rel='stylesheet' type='text/css'>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
	<!--[if lt IE 10]>
		<link href= "css/ie.css" rel= "stylesheet" media= "all" />
	<![endif]-->
	<!--[if lt IE 9]>
		<script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
	<![endif]-->
	<link rel="stylesheet" href="{{ resourceUrl 'css/base.css' }}">
	<style type="text/css">
		::selection {
			background-color: {{ theme.colors.brandColor }};
			color: {{ theme.colors.navItemColor }};
		}
		::-moz-selection {
			background-color: {{ theme.colors.brandColor }};
			color: {{ theme.colors.navItemColor }};
		}
		a,
		.logout:hover,
		#slidecaption h2,
		#slidecaption em,
		#slidecaption strong,
		.option-set li a:hover,
		.option-set li a.selected {
			color: {{ theme.colors.brandColor }};
		}
		nav#mainnav,
		.project-wrp:hover,
		.copyright {
			background-color: {{ theme.colors.brandColor }};
		}
		.btn-primary,
		.btn-primary:link,
		.btn-primary:hover,
		.btn-primary:active,
		.btn-primary:focus {
			background-color: {{ theme.colors.brandColor }} !important;
		}
		a,
		hr,
		hr.mini {
			border-bottom: 1px solid {{ theme.colors.brandColor }};
		}
		.logout,
		nav #fluid-nav li a,
		.project-wrp:hover .project-name,
		.project-wrp:hover .project-client,
		.project-wrp:hover .project-date {
			color: {{ theme.colors.navItemColor }};
		}
		.project-wrp:hover hr {
			border-color: {{ theme.colors.navItemColor }};
		}
		nav #fluid-nav li a:hover {
			color: {{ theme.colors.navItemRolloverColor }};
		}
		nav #fluid-nav li.current a {
			color: {{ theme.colors.navItemSelectedColor }};
		}

		{{#if theme.images.headerLogo}}
		header .logo {
			background-image: url('{{{ theme.images.headerLogo }}}');
		}
		{{/if}}

		{{#if theme.images.navLogo}}
		nav#mainnav .logo {
			background-image: url('{{{ theme.images.navLogo }}}');
		}
		{{/if}}
	</style>
	<style type="text/css">
{{{ theme.scripts.css }}}
	</style>
</head>

<body>

	<div class="wrapper">

		<nav id="mainnav">
			<div class="container">
				<div class="row">
					<div class="span4">
						{{#if theme.images.headerLogo}}
						<a class="logo"{{#if theme.content.contactWebsite}} href="{{ theme.content.contactWebsite }}"{{/if}}></a>
						{{/if}}
					</div>
					<div class="span8">
					</div>
				</div>
			</div>
		</nav>

		<section class="page">
			<div class="container">
				<div class="row">
					<form class="form-signin" role="form" action="{{ loginUrl }}" method="POST" autocomplete="off" style="text-align: center;">
						<h2 class="form-signin-heading">Sign in</h2>
						<div><input type="text" name="username" class="form-control" placeholder="Username" required autofocus></div>
						<div><input type="password" name="password" class="form-control" placeholder="Password" required></div>
						<div><button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button></div>
					</form>
				</div>
			</div>
		</section>

		<footer>
			<div class="copyright">
				<p class="copyright-message">Powered by <a href="https://www.shunt.io/">Shunt.io</a></p>
			</div>
		</footer>

	</div>

</body>

</html>
