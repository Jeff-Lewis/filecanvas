<h1 class="page-header">Your sites</h1>

{{#if session.sites}}

<p class="lead">The following sites have been registered within this organization:</p>
<div class="row">
	<div class="col-sm-8 col-lg-5">
		<table class="table table-bordered table-condensed">
			<tbody>
				{{#each session.sites }}
				<tr>
					<td>
						<div class="pull-right">
							<a class="btn btn-default btn-xs" href="{{ ../session.urls.sites }}/edit/{{ alias }}" title="Edit this site’s details"><span class="glyphicon glyphicon-pencil"></span> Edit site</a>
							<a class="btn btn-success btn-xs" href="#" title="Dropkick scans your Dropbox folder automatically. Use this link to manually trigger a re-scan of your Dropbox folder."><span class="glyphicon glyphicon-refresh"></span> Sync</a>
						</div>
						{{ name }}
						<a href="{{ ../session.urls.organization }}{{ alias }}" target="_blank" title="Visit this site"><span class="glyphicon glyphicon-new-window"></span></a>
					</td>
				</tr>
				{{/each}}
			</tbody>
		</table>
		<p><a class="btn btn-primary" href="{{ session.urls.sites }}/add"><span class="glyphicon glyphicon-plus"></span> Create a new site</a></p>
	</div>
</div>

{{else}}

<div class="alert alert-info">
	<p>There are no sites registered within this organization.</p>
	<p>Start using Dropkick by <a href="{{ session.urls.sites }}">adding a new site</a>.</p>
</div>

{{/if}}
