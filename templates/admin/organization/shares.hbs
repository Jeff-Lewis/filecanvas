<h1 class="page-header">Your Dropbox folders</h1>

{{#if session.organization.shares}}

<p class="lead">The following Dropbox folders have been linked to this organization:</p>
<div class="row">
	<div class="col-sm-6 col-lg-4">
		<table class="table table-bordered table-condensed">
			<tbody>
				{{#each session.organization.shares }}
				<tr>
					<td>
						<form class="pull-right" action="{{ ../session.urls.organizationShares }}/{{ alias }}" method="POST">
							<input type="hidden" name="_method" value="DELETE"/>
							<input type="hidden" name="share" value="{{ alias }}"/>
							<button type="submit" class="pull-right btn btn-default btn-xs" title="Prevent Shunt from accessing this folder"><span class="glyphicon glyphicon-remove"></span> Unlink folder</button>
						</form>
						{{ name }}
					</td>
				</tr>
				{{/each}}
			</tbody>
		</table>
	</div>
</div>

{{else}}

<div class="alert alert-info">
	<p>There are no Dropbox folders linked to this organization.</p>
	<p>Link a Dropbox folder to this organization by sharing the folder with <strong>incoming@shunt.io</strong>.</p>
</div>

{{/if}}
